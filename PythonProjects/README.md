# OffFactory Python Projects

Run any project with Python 3.11+:

```text
python code_editor.py
python calculator.py
python world_clock.py
```

The code editor opens and saves local code files. Its **Run Python** action
executes only the currently open `.py` file, so use it only with code you trust.
